package net.godlycow.org.commands.config;

import org.bukkit.plugin.Plugin;

/**
 * Configuration for /enderchest command
 * Permission: serveressentials.command.enderchest (hardcoded)
 * Others Permission: serveressentials.command.enderchest.others (hardcoded)
 */
public final class EnderChestConfig {
    // Reserved for future options like cooldown

    public EnderChestConfig(Plugin plugin) {
        // Example: this.cooldownSeconds = plugin.getConfig().getInt("enderchest.cooldown", 0);
    }
}