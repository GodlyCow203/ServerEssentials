package net.lunark.io.commands.config;

import org.bukkit.plugin.Plugin;

public final class TeleportWorldConfig {
    // Configuration stays clean with only non-permission settings
    public TeleportWorldConfig(Plugin plugin) {
        // No permission fields to load
    }
}