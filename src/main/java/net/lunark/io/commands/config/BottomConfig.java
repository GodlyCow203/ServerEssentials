package net.lunark.io.commands.config;

import org.bukkit.plugin.Plugin;

/**
 * Configuration for /bottom command
 * Permission: serveressentials.command.bottom (hardcoded)
 */
public final class BottomConfig {
    // Reserved for future configuration options

    public BottomConfig(Plugin plugin) {
        // Blank - no configurable options yet
    }
}