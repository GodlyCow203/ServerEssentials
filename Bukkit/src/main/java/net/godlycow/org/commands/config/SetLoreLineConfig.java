package net.godlycow.org.commands.config;

import org.bukkit.plugin.Plugin;

/**
 * Configuration for /setloreline command
 * Permission: serveressentials.command.setloreline (hardcoded)
 */
public final class SetLoreLineConfig {
    // Reserved for future options like max-lore-length, cooldown, etc.

    public SetLoreLineConfig(Plugin plugin) {
        // Example: this.maxLoreLength = plugin.getConfig().getInt("setloreline.max-length", 100);
    }
}