package net.godlycow.org.commands.config;

import org.bukkit.plugin.Plugin;

/**
 * Configuration for /track command
 * Permission: serveressentials.command.track (hardcoded)
 */
public final class TrackConfig {
    // Reserved for future configuration options like cooldown, max-range, etc.

    public TrackConfig(Plugin plugin) {
        // Example: this.cooldownSeconds = plugin.getConfig().getInt("track.cooldown", 0);
    }
}