package net.godlycow.org.commands.config;

import org.bukkit.plugin.Plugin;

/**
 * Configuration for /playtime command
 * Permission: serveressentials.command.playtime (hardcoded)
 */
public final class PlaytimeConfig {
    // Reserved for future options like showing days instead of hours

    public PlaytimeConfig(Plugin plugin) {
        // Example: this.showInDays = plugin.getConfig().getBoolean("playtime.show-days", false);
    }
}